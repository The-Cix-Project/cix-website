# Production example for a Cix website VM.
# Replace the hostname and root path before enabling.
cix.example.org {
    root * /srv/cix-website/site
    encode zstd gzip

    header {
        # Static site: no framing, MIME sniffing, or referrer leakage.
        Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self'; script-src 'self'; connect-src 'self' http: https:; object-src 'none'; base-uri 'self'; frame-ancestors 'none'"
        Referrer-Policy "strict-origin-when-cross-origin"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        Cache-Control "public, max-age=300"
    }

    @immutable path /assets/* /cix-release.pub
    header @immutable Cache-Control "public, max-age=31536000, immutable"

    file_server
}
