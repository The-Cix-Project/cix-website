name: Cix website checks
on: [push, pull_request]
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static site quality
        run: python3 scripts/check-site.py
